# 更新日志

本项目遵循语义化版本。

## v0.4.0（2026-09-09）

### 新增

- **答题卷（kind=1）**：控制台「+ 新建答题」创建，支持：
  - **倒计时**：可配置分钟数（0 不限时），填答端倒计时条，到时自动交卷；
  - **自动判分**：单选 / 多选 / 下拉 / 填空四类题型可设正确答案与分值（1-1000），
    服务端判分（填空忽略首尾空格与大小写、多选须全中），未作答计 0 分；
  - **答案展示开关（仅分页模式）**：勾选后为**逐题提交**模式 —— 每题作答完点「提交本题」，
    该题立即判分并展示正确答案与本题得分，随后锁定不可再修改；正确答案仍不下发到填答端，
    仅在提交本题后由服务端逐题回传；列表展示下该开关不可用；
  - **列表 / 分页两种展示方式（互斥）**：分页模式一题一页带上一题/下一题导航与进度；
  - **答题卡（左侧，5 列题号，作答期间常显）**：列表与分页作答时左侧均常驻答题卡（与题目同一行起始、
    与题目留出间距），题号按 5 列排布，顶部显示进度，题号过多时面板内滚动；
    逐题提交模式下题号颜色跟随本题对错（绿 = 正确、红 = 错误），其余模式标记已答 / 未答；
    分页模式点击题号切换题目，列表模式点击题号滚动定位到该题，滚动时自动高亮当前题号；
    手机端答题卡移到题目上方通栏（仍为 5 列）并限高；逐题全部提交后可「查看成绩」查看总分与排行榜，
    倒计时结束自动交卷；
  - **题库**：`/banks` 题库管理页（建库、改名、删除、库内题目增删改），答题卷编辑时可
    「从题库选题」勾选题目导入，题目排列可选「顺序 / 随机」（随机时每位答题者顺序不同）；
  - **答题前个人信息收集（可配置）**：开关 + 自定义字段（最多 5 个，可设必填），
    存入答卷，明细与 CSV 导出可见；
  - **答题后排行榜（可配置）**：得分降序、耗时升序，公开链接可看，展示名取个人信息首字段，
    未填则显示「匿名」；
  - 统计页新增「平均得分」卡片，明细表新增得分与个人信息列；答题卷复制保留配置与题目。
- **AI 支持答题与题库**（沿用统一配额，管理员配置任意 OpenAI 兼容服务后可用）：
  - AI 面板新增「创建答题」标签：一句话生成整份答题卷 —— 题目限定可判分的单选/多选/下拉/填空，
    自动带分值（1-1000）与正确答案（单选/下拉为选项 id、多选为 id 数组、填空为可接受文本数组），
    并生成答题配置（倒计时、展示答案、列表/分页、顺序/随机、排行榜等）；
    生成结果预览后一键创建为答题卷草稿；
  - 题库管理页新增「AI 生成题目」：一句话生成一组带分值/答案的题目，勾选后批量导入当前题库；
  - Agent 对话编辑答题卷时自动按答题规则校验（题型限制、分值、正确答案），
    修改提案不满足规则会被拒绝并要求 AI 修正。

### 数据库

- v3 迁移：`surveys` 增加 `kind`、`quiz_config`；`responses` 增加 `profile`、`score`；
  新增 `question_banks`、`bank_questions` 表。

## v0.3.0（2026-09-05）

### 新增

- **问卷在任意状态均可直接编辑保存**：「发布中」「已停止」的问卷不再要求先停止后复制，
  编辑器顶栏对全部状态开放「保存」按钮，保存后状态保持不变。
- **保存改为按题目 ID 原地更新**：已存在的题目保留原 ID（历史答卷的题目关联与统计不失效），
  ID 缺失或为 0 的题目新增，本次未保留的旧题目删除；乐观锁（updated_at 版本比对）保持不变。
- AI 面板应用提案与「单题 AI 优化」同样保留题目 ID，避免重存导致历史答卷关联丢失。

### 修复

- **分页填答时，作答任意一题会把后续所有题目一次性显示出来**：填答端 `recalcVisibility`
  （逻辑跳转显隐）原先直接改写 `display`，覆盖了分页的按页隐藏；现统一为
  「当前页 ∩ 条件可见」决定显示，作答、逻辑跳转与分页可同时正确工作，
  进入下一页后上一页题目不再展示（可点「上一页」返回）。
- **预览问卷没有分页切换按钮**：预览模式原先会清空操作栏（只留提示文字），
  把 `renderPage` 生成的分页导航一并删掉；现提示移到题目区上方，预览可正常
  逐页查看；预览末页按钮显示「结 束」并禁止真实提交（原实现点击「提交」会
  真实写入一份答卷）。
- **分页问卷填答端启动报错**：`renderPage` 重建操作栏后 `submitBtn` 已不存在，
  `boot` 直接绑定监听会抛 `TypeError`；改为判空绑定（分页问卷的提交走导航按钮）。
- **移动端分页导航挤压且按钮大小不一**：原 `.fill-actions .btn-primary`（最小宽
  220px）误命中导航里的「下一页」导致一大一小，且 `.page-nav` 单行不换行在窄屏
  被挤压；现改为上一页/下一页等宽同行（`flex:1 1 0%` + `min-width:0`），手机端
  页码独立一行置顶，桌面端保持「按钮 · 页码 · 按钮」同行（真机 Chromium 375/320/
  1200px 三档视口实测通过）。

## v0.2.1（2026-09-04）

阻断性缺陷修复。

### 修复

- **经反向代理 / 网关访问时，注册与登录被拒绝（`403 跨站请求被拒绝`）**。
  CSRF 原按 `Origin` 与服务端 `r.Host` 做字符串精确比较；一旦请求经过 fnOS 应用中心网关、
  Nginx 或自定义域名反代，浏览器侧 origin 与应用实际监听地址不一致即被误杀，
  导致新装应用无法创建管理员账号。
  现改为优先采用浏览器 `Sec-Fetch-Site` 头判定——该头由浏览器按「发起方页面 vs 请求目标」
  的真实关系计算、JS 无法伪造，且不受代理改写 Host 影响；真正的跨站请求仍被拒绝。

### 新增

- `ALLOWED_ORIGINS`：额外受信任来源白名单（逗号分隔），用于外部站点嵌入投稿等场景
- `TRUST_PROXY`：老浏览器（不发送 `Sec-Fetch-Site`）走反代时，按 `X-Forwarded-Host` 还原外部地址
- CSRF 单元测试 11 项，覆盖反代、自定义域名、跨站拦截、白名单覆盖、默认端口归一化等场景
- `.gitattributes` 强制 LF，避免 Windows `autocrlf` 把 fpk 内 bash 脚本转成 CRLF 而在 fnOS 上执行失败

## v0.2.0（2026-08-22）

二期功能全量落地。

### 新增

- 管理员后台：总览统计、用户封禁/解封（立即吊销会话）、全站问卷下架/删除（/admin）
- 逻辑跳转：题目可设显示条件（依赖前面的单选/下拉题选项），填答端实时显隐，后端同样推导校验
- 问卷分页：题目级「此后分页」标记，填答端逐页作答、逐页必答校验
- 新题型：日期题、矩阵题（行×列单选）、排序题（上移/下移排序），统计与 CSV 导出同步支持
- 模板库：内置 5 套模板（员工满意度/活动报名/产品反馈/课程评价/消费调研），一键创建草稿
- 统计协作：问卷统计只读分享令牌，/share/{token} 匿名查看统计图表，可随时关闭
- AI 摘要：文本题统计页一键生成答案要点归纳（计入每日配额）
- 体验与修复：AI 入口合并为「AI 助手」右侧停靠面板；SSE 流式响应兼容；前端资源 no-cache；
  移动端顶栏/抽屉/按钮多轮布局修复；AI 长任务超时放宽

## v0.1.0（2026-08-22）

首个完整版本。

### 新增

- 多用户注册 / 登录 / 退出 / 修改密码（bcrypt + Cookie Session，登录失败锁定）
- 问卷管理：创建、卡片式编辑、六种题型、必答与选项设置、上移下移、预览、复制、软删除
- 发布控制：草稿 / 发布中 / 已停止状态机，回收截止时间与回收量上限自动停止，乐观锁保存
- 填答端：匿名链接、二维码分享、必答校验定位、感谢页、按 IP 限频
- 统计：逐题图表、评分均值、开放题列表、单份答卷删除、明细与汇总 CSV 导出
- AI 能力：管理员配置 OpenAI 兼容服务（Key 加密存储、连接测试、每日配额），AI 创建问卷、Agent 对话式改题（提案模式）、单题优化
- 响应式双端适配（wjcat 式顶栏 + 侧栏 + 主区布局，移动端抽屉侧栏）
- 工程配套：schema 迁移、go:embed 单二进制、造数据脚本、备份脚本、全量测试
