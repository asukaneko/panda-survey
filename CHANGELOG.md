# 更新日志

本项目遵循语义化版本。

## v0.3.0（2026-09-05）

### 新增

- **问卷在任意状态均可直接编辑保存**：「发布中」「已停止」的问卷不再要求先停止后复制，
  编辑器顶栏对全部状态开放「保存」按钮，保存后状态保持不变。
- **保存改为按题目 ID 原地更新**：已存在的题目保留原 ID（历史答卷的题目关联与统计不失效），
  ID 缺失或为 0 的题目新增，本次未保留的旧题目删除；乐观锁（updated_at 版本比对）保持不变。
- AI 面板应用提案与「单题 AI 优化」同样保留题目 ID，避免重存导致历史答卷关联丢失。

## v0.2.1（2026-09-04）

阻断性缺陷修复。

### 修复

- **经反向代理 / 网关访问时，注册与登录被拒绝（`403 跨站请求被拒绝`）**。
  CSRF 原按 `Origin` 与服务端 `r.Host` 做字符串精确比较；一旦请求经过 fnOS 应用中心网关、
  Nginx 或自定义域名反代，浏览器侧 origin 与应用实际监听地址不一致即被误杀，
  导致新装应用无法创建管理员账号。
  现改为优先采用浏览器 `Sec-Fetch-Site` 头判定——该头由浏览器按「发起方页面 vs 请求目标」
  的真实关系计算、JS 无法伪造，且不受代理改写 Host 影响；真正的跨站请求仍被拒绝。

### 新增

- `ALLOWED_ORIGINS`：额外受信任来源白名单（逗号分隔），用于外部站点嵌入投稿等场景
- `TRUST_PROXY`：老浏览器（不发送 `Sec-Fetch-Site`）走反代时，按 `X-Forwarded-Host` 还原外部地址
- CSRF 单元测试 11 项，覆盖反代、自定义域名、跨站拦截、白名单覆盖、默认端口归一化等场景
- `.gitattributes` 强制 LF，避免 Windows `autocrlf` 把 fpk 内 bash 脚本转成 CRLF 而在 fnOS 上执行失败

## v0.2.0（2026-08-22）

二期功能全量落地。

### 新增

- 管理员后台：总览统计、用户封禁/解封（立即吊销会话）、全站问卷下架/删除（/admin）
- 逻辑跳转：题目可设显示条件（依赖前面的单选/下拉题选项），填答端实时显隐，后端同样推导校验
- 问卷分页：题目级「此后分页」标记，填答端逐页作答、逐页必答校验
- 新题型：日期题、矩阵题（行×列单选）、排序题（上移/下移排序），统计与 CSV 导出同步支持
- 模板库：内置 5 套模板（员工满意度/活动报名/产品反馈/课程评价/消费调研），一键创建草稿
- 统计协作：问卷统计只读分享令牌，/share/{token} 匿名查看统计图表，可随时关闭
- AI 摘要：文本题统计页一键生成答案要点归纳（计入每日配额）
- 体验与修复：AI 入口合并为「AI 助手」右侧停靠面板；SSE 流式响应兼容；前端资源 no-cache；
  移动端顶栏/抽屉/按钮多轮布局修复；AI 长任务超时放宽

## v0.1.0（2026-08-22）

首个完整版本。

### 新增

- 多用户注册 / 登录 / 退出 / 修改密码（bcrypt + Cookie Session，登录失败锁定）
- 问卷管理：创建、卡片式编辑、六种题型、必答与选项设置、上移下移、预览、复制、软删除
- 发布控制：草稿 / 发布中 / 已停止状态机，回收截止时间与回收量上限自动停止，乐观锁保存
- 填答端：匿名链接、二维码分享、必答校验定位、感谢页、按 IP 限频
- 统计：逐题图表、评分均值、开放题列表、单份答卷删除、明细与汇总 CSV 导出
- AI 能力：管理员配置 OpenAI 兼容服务（Key 加密存储、连接测试、每日配额），AI 创建问卷、Agent 对话式改题（提案模式）、单题优化
- 响应式双端适配（wjcat 式顶栏 + 侧栏 + 主区布局，移动端抽屉侧栏）
- 工程配套：schema 迁移、go:embed 单二进制、造数据脚本、备份脚本、全量测试
